<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here.-->
    </style>

    <style name="CustomTitlePageIndicator">

        <!-- color de fondo de toda la barra en la que se muestra el nombre -->
        <item name="android:background">#eaeaea</item>
        <!-- color de la barra indicadora, debajo de los nombres -->
        <item name="footerColor">#b2cb39</item>
        <!-- ancho linea inferior del footer -->
        <item name="footerLineHeight">2dp</item>
        <!-- ancho de la barra inferior al nombre -->
        <item name="footerIndicatorHeight">4dp</item>
        <!-- estilo del indicador inferior "none","triangle","underline" -->
        <item name="footerIndicatorStyle">underline</item>
        <!-- color del texto -->
        <item name="android:textColor">@android:color/black</item>
        <!-- color del texto de la página seleccionada -->
        <item name="selectedColor">@android:color/black</item>
        <!-- aplica bold al texto seleccionado -->
        <item name="selectedBold">true</item>
    </style>


    <style name="CustomTabPageIndicator" parent="Widget.TabPageIndicator">
        <item name="android:textAppearance">@android:style/TextAppearance.Medium</item>

        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">15sp</item>

        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:fadingEdge">horizontal</item>
        <item name="android:fadingEdgeLength">8dp</item>
    </style>

    <style name="CustomCirclePageIndicator">
        <item name="fillColor">#FF888888</item>
        <item name="strokeColor">#FF000000</item>
        <item name="strokeWidth">2dp</item>
        <item name="radius">10dp</item>
        <item name="centered">true</item>
    </style>
</resources>


